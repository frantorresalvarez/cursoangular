ng generate component menu (Para generar un componente ya sea menu cabecera piedepagina..)

ng serve para poner en funcionamiento el servidor(localhost:4200)

ng build (para hacer un build del proyecto que estará en dist)